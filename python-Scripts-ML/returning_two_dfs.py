

import pandas as pd
import numpy as np

def return_results():

    # random dataframes
    # date = pd.date_range('today', periods=20)
    # df1 = #pd.DataFrame(np.random.rand(20, 3), index=date, columns=list('xyz'))
    # df2 = #pd.DataFrame(np.random.rand(20, 4), index=date, columns='A B C D'.split())
    
    matched = full_row_df
    unmatched = unmatched_wines_df

    return matched, unmatched


matched_results, unmatched_results = return_results()





